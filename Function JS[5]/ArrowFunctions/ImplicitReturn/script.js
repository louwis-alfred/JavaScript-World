// You can omit "return" keyword to "()" if the function body is a single expression

// Example # 1
const rollDie = () => (
    Math.floor(Math.random() * 6) + 1
)

// Example # 2
const add = (x,y) => (
    x + y
)